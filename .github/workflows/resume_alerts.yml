name: Resume Alerts

# Run manually from the Actions tab (the big "Run workflow" button).
on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  resume:
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set paused = false in config.json
        run: |
          python3 - <<'EOF'
          import json, pathlib
          p = pathlib.Path("cat_checker/config.json")
          cfg = json.loads(p.read_text())
          cfg["paused"] = False
          p.write_text(json.dumps(cfg, indent=2) + "\n")
          print("paused set to false")
          EOF

      - name: Commit and push
        run: |
          git config user.name  "cat-alert-bot"
          git config user.email "actions@github.com"
          git add cat_checker/config.json
          git commit -m "chore: resume alerts [skip ci]"
          git pull --rebase origin ${{ github.ref_name }}
          git push origin ${{ github.ref_name }}
